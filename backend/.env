# ============================================================
# Backend Environment Configuration
# ============================================================

# Frontend origin URL (used as fallback when generating public surprise links)
# Change this to your production domain when deploying
FRONTEND_BASE_URL=http://localhost:5173
